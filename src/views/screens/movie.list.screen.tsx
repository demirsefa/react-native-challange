import {View} from "react-native";
import GeneralProps from "../../props/general.props";

export default function MovieListScreen({children}: GeneralProps) {

    return <View>
        {children}
    </View>
}