import {View} from "react-native";
import GeneralProps from "../../props/general.props";

export default function MovieDetailsScreen({children}: GeneralProps) {

    return <View>
        {children}
    </View>
}